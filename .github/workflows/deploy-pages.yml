name: Deploy — Loop Messenger › CF Pages [DISABLED]

  # RETIRED: The old @workspace/loop-messenger CF Pages frontend has been replaced by
  # loop-messenger-ui-ux (chat.rald.cloud CF Worker, deployed from the loop-messenger-ui-ux repo).
  # This workflow is disabled to prevent stale deployments.
  on:
    workflow_dispatch:

  jobs:
    deploy:
      name: "[DISABLED] — see loop-messenger-ui-ux repo"
      runs-on: ubuntu-latest
      if: false
      steps:
        - run: echo "This workflow is retired"
  