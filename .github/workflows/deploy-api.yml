name: Deploy — Loop Messenger API › Fly.io [DISABLED]

  # RETIRED: Fly.io deployment is no longer used.
  # The Loop Messenger API is deployed via deploy-worker.yml to messenger.rald.cloud (CF Worker).
  # FLY_API_TOKEN was always empty anyway — this workflow never ran successfully.
  on:
    workflow_dispatch:

  jobs:
    deploy:
      name: "[DISABLED] — see deploy-worker.yml"
      runs-on: ubuntu-latest
      if: false
      steps:
        - run: echo "This workflow is retired"
  